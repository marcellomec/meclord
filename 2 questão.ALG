Algoritmo "sucessor"
Var
n1, mult, ant:real


Inicio
escreval("insira o numero inicial")
leia(n1)

ant <- (n1 - 1);
mult <- (n1) * (n1+1);

escreval("O antecessor do numero e ", ant," e o sucessor multiplicado e",mult)a


Fimalgoritmo